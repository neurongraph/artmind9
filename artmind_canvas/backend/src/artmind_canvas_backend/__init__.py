"""artmind_canvas backend — a pure client of artmind (ADR 0003).

Reuses artmind's dependency-free agent-harness seams and adds the canvas's own
richer contract (the ``render`` event, ADR 0014) via a thin wrapper backend.
"""
