# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# OS
.DS_Store
worker.pid

# Local config & secrets
.env
.pi/
.claude/

# Runtime data
data/document_registry.db
data/refine
data/documents
data/graph_snapshot
# Keep data.kg in github, but exclude sensitive/private domains
data/kg/sales_collateral
data/kg/project_governance
data/kg/contracts
graphify-out/
logs/
# Root-only Node/opencode config (leading slash: do NOT match nested manifests
# like artmind_canvas/frontend/package.json, which must be committed).
/package.json
/package-lock.json
/opencode.json
.agents/skills/
.pytest_cache/
